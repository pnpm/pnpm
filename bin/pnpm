#!/usr/bin/env sh
cmd=$1
shift
args=$*

case $cmd in
  h | help | -h | --help)
    shift
    cmd=$args
    args=--help
    ;;
esac

case $cmd in
  i)
    cmd=install
    ;;
esac

if which pnpm-${cmd} 2>&1 >/dev/null; then
  pnpm-${cmd} $args
else
  npm ${cmd} $args
fi
