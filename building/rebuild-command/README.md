# @pnpm/building.rebuild-command

> Rebuild command implementation for pnpm

Provides the `pnpm rebuild` CLI command handler and its recursive workspace variant. Extracted from `@pnpm/building.commands` to break a circular dependency with `@pnpm/installing.commands`.
