# https://github.com/crate-ci/typos
# cargo install typos-cli
# typos

[files]
extend-exclude = [
  "pnpm/crates/crypto-shasums-file/src/node_release_keys.rs",
  # Verbatim registry packument snapshot; its base64 signature blobs trip
  # typos' dictionary.
  "pnpm/tasks/micro-benchmark/fixtures/lodash.json",
]

[default.extend-words]
cafs   = "cafs"
ect    = "ect"
intoto = "intoto"
pn     = "pn"

[default.extend-identifiers]
PnP = "PnP"
