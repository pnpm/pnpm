---
"@pnpm/plugin-commands-deploy": minor
"@pnpm/types": minor
"pnpm": minor
---

Create **pnpm eject** command to export dependencies marked with ejected: true to an isolated directory. This enables creating minimal production bundles for serverless environments by isolating only the necessary native or heavy dependencies
