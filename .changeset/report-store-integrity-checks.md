---
"pacquet": patch
---

An install that had to re-verify the content of more than 1000 files in the store now says so, so a slow install has a visible cause: `The integrity of N files was checked. This might have caused installation to take longer.`
