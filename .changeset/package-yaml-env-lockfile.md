---
"pacquet": patch
---

A project whose manifest is `package.yaml` now records its pinned pnpm in `pnpm-lock.yaml` under `packageManagerDependencies`, the entry a `package.json` project already gets [pnpm/pnpm#15167](https://github.com/pnpm/pnpm/issues/15167).
