---
"pacquet": minor
---

`pnpm install` now reads dynamic Python project metadata from the build backend. Projects with only a `requirements.txt` file now get a Python environment and lockfile. Related to [pnpm/pnpm#14945](https://github.com/pnpm/pnpm/issues/14945).
