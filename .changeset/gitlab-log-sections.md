---
"@pnpm/log.group": patch
"pnpm": patch
---

`pnpm run --recursive` now prints GitLab CI collapsible sections that GitLab recognizes. The section markers used to appear as raw text in the job log.
