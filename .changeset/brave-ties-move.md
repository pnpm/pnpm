---
"@pnpm/plugin-commands-init": minor
"@pnpm/config": minor
"pnpm": minor
---

Add a flag called `--init-preset <package|application>` (and additional aliases) to `pnpm init` to set preset of fields of `package.json` [#10226](https://github.com/pnpm/pnpm/issues/10226).
