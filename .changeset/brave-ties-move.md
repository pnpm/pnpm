---
"@pnpm/plugin-commands-init": minor
"@pnpm/config": minor
"pnpm": minor
---

Add a flag called `--init-bare` (aliases: `--bare`, `-B`) to `pnpm init` to skip fields not necessary for an application project in `package.json` [#10226](https://github.com/pnpm/pnpm/issues/10226).
