---
"pnpm": patch
"pacquet": patch
---

Environment variables are now expanded in `_auth` `authToken` values loaded from global `config.yaml` and environment variables [pnpm/pnpm#12828](https://github.com/pnpm/pnpm/issues/12828).
