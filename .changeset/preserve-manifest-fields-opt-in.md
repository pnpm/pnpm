---
"@pnpm/releasing.commands": minor
"@pnpm/releasing.exportable-manifest": minor
"@pnpm/config.reader": minor
"pnpm": minor
---

Add a `preserve-manifest-fields` option for `pnpm pack` and `pnpm publish` that keeps the original manifest fields in packed and published manifests.
