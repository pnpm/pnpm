---
"pacquet": patch
---

On Windows, a dependency's build script now runs on every install when the build changes nothing inside the package directory and the package ships an executable file [#15667](https://github.com/pnpm/pnpm/issues/15667).
