---
"pacquet": minor
---

`pnpm install` now lists the patched dependencies it installed, one `name@version ✔` line per package under "Applied patches:" [#7613](https://github.com/pnpm/pnpm/issues/7613).
