---
"pacquet": patch
---

Installs in different projects that share a global virtual store no longer fail on Windows with `Cannot create a file when that file already exists` while linking the same dependency.
