---
"pacquet": minor
---

`pnpm version` now also sets the `version` field of a `jsr.json` or `jsr.jsonc` next to each bumped `package.json`, keeping the file's formatting and comments. The version commit includes the file [#8317](https://github.com/pnpm/pnpm/issues/8317).
