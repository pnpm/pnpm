---
"pacquet": minor
---

Added the `metadataCache` setting. Run a command with `--no-metadata-cache` to fetch registry metadata from the network without reading or writing the metadata cache on disk [pnpm/pnpm#5357](https://github.com/pnpm/pnpm/issues/5357).
