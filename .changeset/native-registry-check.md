---
"@pnpm/deps.inspection.commands": minor
"pnpm": minor
---

Added native `pnpm view` command with `info` and `v` aliases for viewing package information from the registry without relying on npm CLI.
