---
"@pnpm/deps.compliance.commands": minor
"pnpm": minor
"pacquet": minor
---

Added the `cleanupUnusedIgnoredGhsas` audit configuration. When set to `true`, pnpm will remove unused GHSA entries during `audit --fix`.
