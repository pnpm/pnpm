---
"@pnpm/installing.commands": minor
"pacquet": minor
"pnpm": minor
---

Add glob pattern support to `pnpm remove`/`rm`, matching the dependency selector behavior already supported by `pnpm update`.
