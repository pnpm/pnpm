---
"pacquet": minor
---

`pnpm version --json` prints the current package version without changing it. `pnpm -r version none --json` prints the selected workspace package versions [pnpm/pnpm#12650](https://github.com/pnpm/pnpm/issues/12650).
