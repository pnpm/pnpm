---
"pacquet": patch
---

`pnpm install` now accepts Rust git dependencies that omit a package version. pnpm also generates Cargo lockfiles for workspaces with git dependencies [pnpm/pnpm#14944](https://github.com/pnpm/pnpm/issues/14944).
