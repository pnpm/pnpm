---
"pacquet": patch
---

`pnpm install`, `pnpm add`, and `pnpm deploy` now accept `--ignore-platform-checks` to bypass installability checks and `--reinstall` to re-materialize every package slot. `--force` remains as the combination of both.
