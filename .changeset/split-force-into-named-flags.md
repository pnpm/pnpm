---
"pacquet": minor
---

`pnpm install`, `pnpm add`, and `pnpm deploy` now accept `--ignore-platform-checks` to bypass platform compatibility checks and `--reinstall` to re-materialize package slots in `node_modules` [#15094](https://github.com/pnpm/pnpm/issues/15094).

