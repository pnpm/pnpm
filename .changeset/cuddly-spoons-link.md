---
"pacquet": patch
---

`pnpm deploy` now links commands exposed by workspace dependencies into the deployed project's `node_modules/.bin` directory [pnpm/pnpm#14899](https://github.com/pnpm/pnpm/issues/14899).
