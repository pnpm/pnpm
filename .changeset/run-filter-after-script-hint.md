---
"pacquet": patch
---

When `pnpm run` cannot find a script and `--filter` follows the script name, the error now suggests putting the filter option before the script name [#4655](https://github.com/pnpm/pnpm/issues/4655).
