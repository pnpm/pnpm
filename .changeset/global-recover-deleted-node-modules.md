---
"pnpm": patch
"pacquet": patch
---

`pnpm update -g` and `pnpm remove -g` can recover a global package group whose entire `node_modules` directory was deleted [#15093](https://github.com/pnpm/pnpm/issues/15093).
