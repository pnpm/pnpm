---
"pacquet": minor
---

`pnpm pack` now honors `--silent`, `--reporter=silent`, and `--loglevel=silent` to hide the tarball contents and summary. Explicit `--json` output is still printed [#10297](https://github.com/pnpm/pnpm/issues/10297).
