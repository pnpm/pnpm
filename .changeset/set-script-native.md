---
"@pnpm/pkg.commands": minor
"pnpm": minor
---

feat: add native `pnpm set-script` command with `ss` alias

Added a new command to set scripts in package.json:
- `pnpm set-script <name> <command>`
- `pnpm ss <name> <command>` (short alias)
