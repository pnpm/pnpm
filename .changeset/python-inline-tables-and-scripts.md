---
"pacquet": patch
---

`pnpm add pypi:...` supports empty inline Python project and dependency-group tables. Python wheel scripts with Windows line endings use the installed environment's interpreter.
