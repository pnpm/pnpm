---
"@pnpm/deps.compliance.commands": patch
"@pnpm/installing.commands": patch
---

Improve `pnpm audit --fix=update` so vulnerabilities with valid resolvable patched versions are updated more reliably.
