---
"pacquet": patch
---

POSIX shell shims generated by the Rust stack now follow symbolic links before computing `basedir`, preventing execution failures when a shim is invoked via an external symlink on `PATH`.
