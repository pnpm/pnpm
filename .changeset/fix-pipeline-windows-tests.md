---
"pacquet": patch
---

Fixed `pnpm pipeline` on Windows to correctly parse repository paths in watch mode and resolve canonical paths for shared Cargo build caches across worktrees [`pnpm/pnpm#15105`](https://github.com/pnpm/pnpm/issues/15105).
