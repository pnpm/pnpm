---
"pnpm": patch
"pacquet": minor
---

`pnpm doctor` now warns in its "Install method" check when `pnpm` on PATH is not the pnpm in the global bin directory, and names the command that removes the other copy.
